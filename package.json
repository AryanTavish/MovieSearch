{
  "name": "imdb_clone",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.0",
    "@mui/icons-material": "^7.1.0",
    "@mui/material": "^7.1.0",
    "@mui/x-data-grid": "^8.5.1",
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^1.10.0",
    "carousel": "^0.0.3",
    "gsap": "^3.13.0",
    "icons-material": "^0.0.1-security",
    "primeicons": "^7.0.0",
    "primereact": "^10.9.6",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-multi-carousel": "^2.8.6",
    "react-responsive-carousel": "^3.2.23",
    "react-router-dom": "^7.6.1",
    "react-scripts": "^5.0.1",
    "react-toggle-dark-mode": "^1.1.1",
    "styled": "^1.0.0",
    "styled-components": "^6.1.18",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
The Netlify deploy errored, with the following guidance provided:

**Diagnosis:**
The error occurred because the build process failed to fetch cached dependencies.

**Solution:**
Since the build failed to fetch cached dependencies, one solution would be to ensure that the required dependencies are properly listed in the `package.json` file and that all necessary files are committed to the repository. If everything seems to be in order, you may need to investigate the caching settings in the build configuration to ensure they are correctly set up.

The relevant error logs are:

Line 0: build-image version: b825674d3cfa85ad94bf5a8a254e528c1b2a912f (noble)
Line 1: buildbot version: b825674d3cfa85ad94bf5a8a254e528c1b2a912f
Line 2: Fetching cached dependencies
Line 3: Failed to fetch cache, continuing with build
Line 4: Starting to prepare the repo for build
Line 5: No cached dependencies found. Cloning fresh repo
Line 6: git clone --filter=blob:none https://github.com/AryanTavish/MovieForSearch
Line 7: Preparing Git Reference refs/heads/main
Line 8: Starting to install dependencies
}
